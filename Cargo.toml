[workspace]
resolver = "2"
members = [
    "tuidom",
    "rafter",
    "rafter-derive",
    "dataverse-lib",
    "dataverse-cli",
    "dataverse-tui",
    "dataverse-tui-derive",
]

[profile.dist]
inherits = "release"
lto = "thin"

[workspace.lints.clippy]
all = "warn"
